---
description: Research a topic using SearXNG web search
argument-hint: <query>
---

Use the Task tool with `subagent_type="searxngcli:agent"` and `run_in_background=false` to research the following query:

$ARGUMENTS

After the subagent returns, present the findings to the user.
